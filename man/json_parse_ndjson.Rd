% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ndjson.R
\name{json_parse_ndjson}
\alias{json_parse_ndjson}
\alias{json_write_ndjson}
\alias{json_write_ndjson_raw}
\title{Parse and write NDJSON}
\usage{
json_parse_ndjson(x, simplify = TRUE, data_frame = FALSE)

json_write_ndjson(x, auto_unbox = TRUE)

json_write_ndjson_raw(x, auto_unbox = TRUE)
}
\arguments{
\item{x}{For \code{json_parse_ndjson()}, a single string or a raw vector of NDJSON
bytes. For the writers, a list of records or a data frame — a data frame is
written one object per row, which is the shape NDJSON exists for.}

\item{simplify}{Passed through to \code{\link[=json_parse]{json_parse()}} for each record.}

\item{data_frame}{Passed through to \code{\link[=json_parse]{json_parse()}} for each record. Note
that records are parsed one at a time, so this turns an array \emph{inside} a
record into a data frame; it does not make a frame out of the stream.}

\item{auto_unbox}{Passed through to \code{\link[=json_write]{json_write()}} for each record.}
}
\value{
\code{json_parse_ndjson()} returns a list with one element per record.
\code{json_write_ndjson()} returns a single string, and
\code{json_write_ndjson_raw()} a raw vector of UTF-8 bytes. Both end with a
trailing newline, so appending another record is always valid.
}
\description{
NDJSON (\code{application/x-ndjson}, also called JSON Lines) is one JSON value per
line. \code{json_parse_ndjson()} turns such a body into a list of records;
\code{json_write_ndjson()} and \code{json_write_ndjson_raw()} go the other way.
}
\details{
Each line is parsed exactly as \code{\link[=json_parse]{json_parse()}} would parse it, and each record
is written exactly as \code{\link[=json_write]{json_write()}} would write it, so the type mappings
documented there apply unchanged. The result of parsing is \strong{always a list},
one element per record, however uniform the records are — records are
independent documents, and an NDJSON body is not a table.

Blank lines are skipped rather than treated as records, and \verb{\\r\\n} line
endings are accepted. A parse failure reports the \strong{line number}, because
"invalid JSON at byte 41827" is not useful in a body of 10,000 records.
}
\section{Why line framing is safe}{
A raw newline byte cannot appear inside a JSON string — it must be escaped as
\verb{\\\\n} — and zujson's writer always escapes it. A serialized record therefore
never contains a bare newline, so splitting on newlines can never cut a
record in half. This is the property the format rests on.

It is also why there is no \code{pretty} argument: indented JSON contains
newlines, and a newline inside a record is exactly what NDJSON framing cannot
survive. Pretty-printed NDJSON is corrupt, not prettier.
}

\examples{
body <- '{"id":1,"ok":true}\n{"id":2,"ok":false}\n'
json_parse_ndjson(body)

# a data frame is one record per row
cat(json_write_ndjson(data.frame(id = 1:2, nm = c("a", "b"))))

# round trip
recs <- list(list(a = 1L), list(b = "x"))
identical(json_parse_ndjson(json_write_ndjson(recs)), recs)

# bytes, ready to be an HTTP request body
json_write_ndjson_raw(list(list(a = 1L), list(a = 2L)))
}
\seealso{
\code{\link[=json_parse]{json_parse()}} and \code{\link[=json_write]{json_write()}} for single documents.
}
