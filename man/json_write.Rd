% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{json_write}
\alias{json_write}
\alias{json_write_raw}
\title{Serialize R as JSON}
\usage{
json_write(x, pretty = FALSE, auto_unbox = TRUE)

json_write_raw(x, pretty = FALSE, auto_unbox = TRUE)
}
\arguments{
\item{x}{The R object to serialize.}

\item{pretty}{Whether to indent the output. \code{FALSE} (the default) writes the
compact form, which is what you want for a request body.}

\item{auto_unbox}{Whether a length-1 atomic vector becomes a bare JSON
scalar (\code{TRUE}, the default) or a one-element array. Wrap a value in
\code{\link[=I]{I()}} to keep it an array under \code{auto_unbox = TRUE}; that is the escape
hatch for an API field that must always be a list.}
}
\value{
\code{json_write()} returns a single string; \code{json_write_raw()} returns a
raw vector of UTF-8 bytes.
}
\description{
\code{json_write()} renders an R object as JSON text. \code{json_write_raw()} returns
the same JSON as UTF-8 bytes, which is the form an HTTP request body wants.
}
\section{Type mapping}{
\tabular{ll}{
   R \tab JSON \cr
   \code{NULL} \tab \code{null} \cr
   \code{NA}, \code{NaN}, \code{Inf} \tab \code{null} \cr
   \code{list(a = 1, b = 2)} \tab \verb{\{"a":1,"b":2\}} \cr
   \code{list(1, 2)} \tab \verb{[1,2]} \cr
   \code{c(a = 1, b = 2)} \tab \verb{\{"a":1,"b":2\}} \cr
   \code{1:3} \tab \verb{[1,2,3]} \cr
   \code{"a"} \tab \code{"a"} (see \code{auto_unbox}) \cr
   \code{I("a")} \tab \verb{["a"]} \cr
   \code{factor} \tab its level, as a string \cr
   \code{Date} \tab \code{"YYYY-MM-DD"} \cr
   \code{POSIXct} \tab \code{"YYYY-MM-DDTHH:MM:SSZ"}, in UTC \cr
   \code{data.frame} \tab array of one object per row \cr
   \code{list()} \tab \verb{[]} \cr
   \code{structure(list(), names = character())} \tab \code{{}} \cr
}


A vector or list becomes a JSON \strong{object} when every element is named, and
an \strong{array} otherwise. Partial names would produce keys like \code{""}, which is
valid JSON but almost never intended, so a partially named vector is written
as an array and its names are dropped.

Data frames are written row-oriented, because that is what an HTTP API means
by a table. To get the column-oriented form instead, strip the class first:
\code{json_write(as.list(df))}.

Every kind of missing value becomes \code{null}: JSON has no \code{NA}, and \code{NaN} and
\code{Infinity} are not JSON either. \code{POSIXct} is always written as UTC no matter
what its \code{tzone} says, since it is the same instant either way, and
sub-second parts are dropped.

Doubles are written compactly and always read back as the same number, so
\code{0.1} is \code{0.1} rather than \code{0.10000000000000001}. A double that is a whole
number is written without a decimal point at all — R has no integer literal,
so \code{1} is a double, and \code{1.0} is rejected by a schema expecting an integer.

Complex vectors, raw vectors, functions, environments and \code{POSIXlt} have no
sensible JSON form and raise a \code{zujson_unsupported_type} error rather than
being guessed at. (\code{POSIXlt} is a list of 11 broken-down time fields; convert
it with \code{as.POSIXct()} first.) A vector carrying an unrecognised class is
written as its underlying type, which for a matrix means its values in
column-major order with \code{dim} dropped — matrices are not turned into nested
arrays in this version.
}

\examples{
json_write(list(name = "ada", ids = 1:3, ok = TRUE))

# length-1 vectors unbox by default; I() keeps them arrays
json_write(list(tag = "x", tags = I("x")))

# every atomic vector stays an array
json_write(list(tag = "x"), auto_unbox = FALSE)

cat(json_write(list(a = 1, b = list(c = 2)), pretty = TRUE))

json_write(data.frame(id = 1:2, nm = c("a", "b")))

# bytes, ready to be an HTTP request body
json_write_raw(list(q = "search"))
}
\seealso{
\code{\link[=json_parse]{json_parse()}} for the other direction.
}
